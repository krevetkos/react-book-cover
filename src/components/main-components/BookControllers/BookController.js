import React from 'react';
import SizeController from './SizeController/SizeController';

export default class BookController extends React.Component{

    render(){
        return(
            <SizeController></SizeController>
        )
    }
}
